{
  "name": "2.3.http-tcp-udp-servers",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build-http": "rimraf dist/http && tsc",
    "start-http-server": "npm run build-http && node dist/http/server.js",
    "build-tcp" : "rimraf dist/tcp && tsc",
    "start-tcp-server": "npm run build-tcp && node dist/tcp/server.js",
    "build-udp" : "rimraf dist/udp && tsc",
    "start-udp-server": "npm run build-udp && node dist/udp/server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/http-server": "^0.12.1",
    "@types/node": "^18.11.18",
    "dgram": "^1.0.1",
    "rimraf": "^4.1.2",
    "typescript": "^4.9.5"
  }
}
